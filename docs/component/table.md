# Table
- _colorized_

Visual updates to the `table` html element and its children. Uses normal table
css rules so that all the normal table formatting for sizes and alignments
works as expected.

## Attributes

### sticky-header
If set, all `thead` elements, and any `th`s in the body will be sticky
positioned to the edges of the scrolling area.
