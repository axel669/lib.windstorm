# Avatar
Displays an image or text in a circular container. Supports both `<img>` tags
and `<object>` tags as children for the content.

## Component Macros
- $color

[component.md : ../../../../test/preview/examples/avatar.html : ]: #
